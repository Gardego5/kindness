import Expandable from "@components/Expandable";
import styled from "@emotion/styled";

const component = "WeekExpandable";

const classes = {};

const Root = styled(Expandable)``;

export { classes, Root };
